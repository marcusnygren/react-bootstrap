const inlineEditorInstance = (
  <Alert bsStyle="warning">
    <strong>Holy inline editor!</strong> Best check yo self, you're not looking too good.
  </Alert>
);

ReactDOM.render(inlineEditorInstance, mountNode);
