const inplaceEditorInstance = (
  <InplaceEditor bsStyle="warning">
    <strong>Holy inline editor!</strong> Best check yo self, you're looking good.
    <Input
          type="text"
          placeholder="Enter text" />
  </InplaceEditor>
);

ReactDOM.render(inplaceEditorInstance, mountNode);
